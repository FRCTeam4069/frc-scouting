# A script that handles logic and constants common to multiple Python files in the project
# Intended to be imported by a Python 3 script, not executed directly

# File path of the CSV file to save data entries to
DATA_PATH = '/var/www/frc-scouting/data.csv'

# Print the JSON output type, followed by a newline
print('Content-Type: application/json\n')
